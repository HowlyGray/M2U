{
  "rules": {
    ".read": true,
    ".write": true,
    "peers": {
      ".read": true,
      ".write": true,
      "$peerId": {
        ".validate": "newData.hasChildren(['name', 'timestamp', 'online', 'peerId'])",
        ".indexOn": ["timestamp", "online"]
      }
    }
  }
}
